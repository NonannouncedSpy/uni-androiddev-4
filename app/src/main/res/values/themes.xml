<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Uniandroiddev4" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colors -->
        <item name="android:textColor">@color/purple</item>
        <item name="android:editTextColor">@color/purple</item>
        <item name="android:background">@color/dark_purple</item>
        <item name="android:textColorHint">@color/pale_purple</item>
        <!-- Font stuff -->
        <item name="android:fontFamily">serif-monospace</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="Theme.Uniandroiddev4" parent="Base.Theme.Uniandroiddev4" />
</resources>